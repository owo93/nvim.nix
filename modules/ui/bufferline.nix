{
  vim = {
    tabline.nvimBufferline = {
      enable = true;

      setupOpts = {
        options = {
          mode = "buffers";
          show_buffer_close_icons = false;
          show_close_icon = false;
        };
      };
    };
  };
}
